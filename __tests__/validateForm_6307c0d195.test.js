// ********RoostGPT********
/*
Test generated by RoostGPT for test NodeJS-application using AI Type Open AI and AI Model gpt-4o

ROOST_METHOD_HASH=validateForm_68e689af79
ROOST_METHOD_SIG_HASH=validateForm_6307c0d195


*/

// ********RoostGPT********
/**
 * @jest-environment jsdom
 */

// __tests__/custom.test.js

const { JSDOM } = require('jsdom');
const { validateForm } = require('../src/public/javascripts/custom');

describe('validateForm', () => {
  let dom;
  let document;

  beforeAll(() => {
    // Create a new JSDOM instance before all tests
    dom = new JSDOM('<!doctype html><html><body><input id="title" /></body></html>');
    document = dom.window.document;
  });

  beforeEach(() => {
    // Clear the value before each test
    document.getElementById('title').value = '';
  });

  test('should return true when #title is not empty', () => {
    document.getElementById('title').value = 'Example Title';
    expect(validateForm(document)).toBe(true);
  });

  test('should return false when #title is empty', () => {
    document.getElementById('title').value = '';
    expect(validateForm(document)).toBe(false);
  });
});

